import React from "react";

const Instructions = () => (
  <div>
    <div>
      <h1>Instructions</h1>
      <p>To run the art in full-screen mode (similar to a digital photograph), open 
         Google Chrome in kiosk mode and select play.
      </p>
    </div>
  </div>
);

export default Instructions;